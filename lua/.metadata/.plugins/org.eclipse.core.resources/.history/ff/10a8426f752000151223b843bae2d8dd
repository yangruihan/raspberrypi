-- 由于Lua本身不含有类关键字，所以需要用table来模拟类实现

-- 用来新建类实例
function clone(tab)
	
end

-- 定义一个类
People = {}
-- 定义类方法

--function People.sayHi()
--  print("Hello")
--end

-- ".."为字符串连接
People.sayHi = function (self)
	print("Hello "..self.name)
end

